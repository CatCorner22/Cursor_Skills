interface:
  display_name: Double Entry Claims
  short_description: 'Hallucination suppression via structural claim/evidence pairing: every factual claim must balance with a supporting source before output…'
  default_prompt: Use $double-entry-claims for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
