interface:
  display_name: Parallax Depth
  short_description: Estimate reasoning depth from the shift between the current query and recent history (deep/medium/shallow)
  default_prompt: Use $parallax-depth for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
