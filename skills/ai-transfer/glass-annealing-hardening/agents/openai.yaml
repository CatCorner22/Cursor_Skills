interface:
  display_name: Glass Annealing Hardening
  short_description: 'Staged delivery: high temp (everything flexible) → medium (structure locked) → cool (typos only)'
  default_prompt: Use $glass-annealing-hardening for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
