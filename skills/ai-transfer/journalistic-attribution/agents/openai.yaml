interface:
  display_name: Journalistic Attribution
  short_description: 'Source-first generation: retrieve evidence per claim before prose, inline attribution, strip unattributable claims'
  default_prompt: Use $journalistic-attribution for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
