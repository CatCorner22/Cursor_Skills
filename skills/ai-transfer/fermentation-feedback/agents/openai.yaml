interface:
  display_name: Fermentation Feedback
  short_description: 'Mid-generation monitoring: user activity, corrections typing, deadlines, confidence — adjust or abort mid-stream'
  default_prompt: Use $fermentation-feedback for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
