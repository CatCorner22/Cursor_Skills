interface:
  display_name: Score Study Dual Axis
  short_description: Horizontal pass (narrative arc) plus vertical pass (parallel considerations at each step)
  default_prompt: Use $score-study-dual-axis for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
