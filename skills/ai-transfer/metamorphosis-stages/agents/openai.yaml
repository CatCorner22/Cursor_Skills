interface:
  display_name: Metamorphosis Stages
  short_description: Force larva (brainstorm) → pupa (structure) → adult (polish) as separate artifacts
  default_prompt: Use $metamorphosis-stages for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
