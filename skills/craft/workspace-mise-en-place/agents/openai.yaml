interface:
  display_name: Workspace Mise En Place
  short_description: 'Prepare the workspace before execution: files, folders, env, tools, references, and templates in place so work never stops for a missing…'
  default_prompt: Use $workspace-mise-en-place for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
