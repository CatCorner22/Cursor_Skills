interface:
  display_name: Subscribe
  short_description: Wait for external events (GitHub CI results, PR activity, Slack messages, Linear issues) by subscribing with the cursor-subscriptions MCP…
  default_prompt: Use $subscribe for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
