interface:
  display_name: Huggingface Local Models
  short_description: Use to select models to run locally with llama.cpp and GGUF on CPU, Mac Metal, CUDA, or ROCm
  default_prompt: Use $huggingface-local-models for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
