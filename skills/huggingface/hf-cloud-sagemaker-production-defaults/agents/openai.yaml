interface:
  display_name: Hf Cloud Sagemaker Production Defaults
  short_description: Create a SageMaker endpoint (real-time, real-time scale-to-zero, or async) with autoscaling, CloudWatch alarms, and tagging enabled by…
  default_prompt: Use $hf-cloud-sagemaker-production-defaults for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
