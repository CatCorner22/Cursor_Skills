interface:
  display_name: Trl Training
  short_description: Train and fine-tune transformer language models locally with the TRL CLI (SFT, DPO, GRPO, KTO, RLOO, Reward Model)
  default_prompt: Use $trl-training for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
