interface:
  display_name: Review And Ship
  short_description: Review the current branch for bugs, intent fit, and test coverage; run or write tests; commit focused work; open or update a PR
  default_prompt: Use $review-and-ship for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
