interface:
  display_name: v0
  short_description: Vercel's v0 app builder — taking v0 output into a production repo, and driving the v0 Platform API (v2, api.v0.dev/v2) from code with the…
  default_prompt: Use $v0 for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
