interface:
  display_name: Vercel Firewall
  short_description: Vercel Firewall expert guidance — automatic DDoS mitigation, the Vercel WAF (custom rules, IP blocking, managed rulesets, rate limiting),…
  default_prompt: Use $vercel-firewall for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
