interface:
  display_name: Marketplace
  short_description: Vercel Marketplace expert guidance — discovering, installing, and managing third-party integrations via the `vercel integration` CLI
  default_prompt: Use $marketplace for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
