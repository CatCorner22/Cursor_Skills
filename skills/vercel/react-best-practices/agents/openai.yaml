interface:
  display_name: React Best Practices
  short_description: React performance best-practices for TSX files — 64 Vercel rules across 8 impact tiers (waterfalls, bundle size, server/client fetching,…
  default_prompt: Use $react-best-practices for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
