interface:
  display_name: CDN caching
  short_description: Debug Vercel CDN caching — cache hit rate, stale content, revalidation behavior, ISR + PPR, per-request cache reasons (cacheReason) and…
  default_prompt: Use $cdn-caching for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
