interface:
  display_name: Train Sentence Transformers
  short_description: Train or fine-tune sentence-transformers models across `SentenceTransformer` (bi-encoder; dense or static embedding model; for retrieval,…
  default_prompt: Use $train-sentence-transformers for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
