interface:
  display_name: Hf Cloud Aws Context Discovery
  short_description: Discover the user's local AWS context (active profile, region, account ID, caller identity) at the start of any AWS task
  default_prompt: Use $hf-cloud-aws-context-discovery for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
