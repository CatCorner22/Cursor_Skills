interface:
  display_name: PR review canvas
  short_description: Generate an interactive PR review walkthrough as an HTML page
  default_prompt: Use $pr-review-canvas for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
