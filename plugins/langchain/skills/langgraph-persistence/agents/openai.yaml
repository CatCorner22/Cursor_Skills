interface:
  display_name: Langgraph Persistence
  short_description: 'Persist LangGraph state: checkpointers, `thread_id`, time travel over checkpoint history, the cross-thread `Store`, and subgraph…'
  default_prompt: Use $langgraph-persistence for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
