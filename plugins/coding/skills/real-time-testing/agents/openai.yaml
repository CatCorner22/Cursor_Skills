interface:
  display_name: Real Time Testing
  short_description: 'Test code continuously while writing it: red-green-refactor, watch mode, typecheck and lint after every slice, and never report done…'
  default_prompt: Use $real-time-testing for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
