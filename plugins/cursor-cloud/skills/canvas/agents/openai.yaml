interface:
  display_name: Canvas
  short_description: Author standalone .canvas.tsx analytical artifacts (charts, tables, audits, metrics) using the cursor/canvas SDK
  default_prompt: Use $canvas for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
