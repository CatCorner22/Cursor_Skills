interface:
  display_name: Survey Triangulation
  short_description: Require three independent retrieval paths per factual claim; score agreement 3/3, 2/3, or contested
  default_prompt: Use $survey-triangulation for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
