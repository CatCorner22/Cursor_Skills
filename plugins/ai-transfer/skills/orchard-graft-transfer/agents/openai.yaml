interface:
  display_name: Orchard Graft Transfer
  short_description: 'Graft a specialist scion onto a safe rootstock output: keep the trusted base, attach expert additions at a marked join'
  default_prompt: Use $orchard-graft-transfer for this task.
policy:
  allow_implicit_invocation: false
  products:
  - CHAT
  - CODEX
